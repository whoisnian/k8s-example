apiVersion: v1
kind: Service
metadata:
  labels:
    app: k8s-example
    tier: frontend
  name: frontend
spec:
  type: NodePort
  ports:
  - port: 8082
  selector:
    app: k8s-example
    tier: frontend