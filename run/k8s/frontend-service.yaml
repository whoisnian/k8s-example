apiVersion: v1
kind: Service
metadata:
  labels:
    app: k8s-example
    tier: frontend
  name: frontend
spec:
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
  selector:
    app: k8s-example
    tier: frontend