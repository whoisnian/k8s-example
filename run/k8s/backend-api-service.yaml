apiVersion: v1
kind: Service
metadata:
  labels:
    app: k8s-example
    tier: backend-api
  name: backend-api
spec:
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: k8s-example
    tier: backend-api